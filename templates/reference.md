---
description: <!-- One sentence: what this page enumerates. -->
---

<!--
Template: Reference (information)
Use for: exact, complete, look-up-oriented facts: parameters, endpoints,
configuration keys, error codes, limits.
Reference describes; it never instructs ("to do X, …" belongs in a how-to)
and never persuades. Structure mirrors the subject's own structure, so the
reader can predict where a fact lives. Completeness beats narrative: an
undocumented default is a defect. Generate from source where possible and
note the generator here.
Delete all template comments before committing.
-->

# <Subject> reference

<!-- One or two sentences: what this page covers and what it deliberately
     does not (with links). If generated, state the source of truth and
     the generator script. -->

## <Logical grouping>

<!-- One H2 per grouping the subject itself defines (per endpoint, per
     config section, per error class). Uniform columns; every row complete.
     Pick the columns that fit the subject: -->

| Name | Type | Default | Required | Description |
|---|---|---|---|---|
| `<name>` | | | | |

## Limits *(optional)*

<!-- Caps, quotas, maximums — with the behavior when exceeded. -->

## Related

<!-- The concept page explaining this subject; adjacent references. -->
