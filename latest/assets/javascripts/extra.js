/* Zevra documentation — global script extensions.
 *
 * Keep this file empty unless a behavior cannot be achieved with Material
 * configuration or CSS. Any addition must be documented in STYLE_GUIDE.md
 * and must work with `navigation.instant` (re-run on page change via
 * document$ subscription, not DOMContentLoaded). */
